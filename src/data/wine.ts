import { images } from "@/lib/site";

/** Wine experience imagery — centralised for reuse and CMS readiness. */
export const wineExperienceImages = {
  cellar: images.wineCellar,
  detail: "/images/wine/Vinho-detalhe-2.webp",
  hero: images.wine
} as const;
